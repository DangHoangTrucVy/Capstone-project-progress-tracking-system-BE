package com.capstone.tracking.meeting.dto;

/** Normalizes blueprint's "Approve"/"Reject" strings to this codebase's UPPER_CASE enum convention. */
public enum ApprovalDecision {
    APPROVE,
    REJECT
}
